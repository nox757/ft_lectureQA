package fintechqa.web1.elements;

import fintechqa.web1.actions.ActionHelper;
import org.openqa.selenium.WebElement;

public class InputBlock extends BaseElement {

    public InputBlock(WebElement element, ActionHelper actionHelper) {
        super(element, actionHelper);
    }
}
